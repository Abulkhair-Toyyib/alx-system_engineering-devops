#!/bin/bash

# Get the first letter of each line and join them together
decoded_message=$(sed 's/^\(.\).*/\1/' | tr -d '\n')

# Print the decoded message followed by a new line
echo "$decoded_message"

